{
  "name": "quiet-block",
  "private": true,
  "version": "1.0.0",
  "description": "QuietBlock - a quiet personal ad blocker for Chrome (MV3). No telemetry, no promotions, no upsell pages.",
  "license": "MIT",
  "author": "Akuma Huang",
  "type": "module",
  "packageManager": "bun@1.4.2",
  "scripts": {
    "build": "node scripts/build.mjs",
    "icons": "node scripts/gen-icons.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/",
    "check:quiet": "node scripts/check-quiet.mjs",
    "verify": "bun run typecheck && bun run test && bun run build && bun run check:quiet",
    "e2e": "node scripts/e2e.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "@types/node": "^26.6.2",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
